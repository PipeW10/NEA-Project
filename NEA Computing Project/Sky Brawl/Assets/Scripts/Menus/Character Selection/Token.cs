using UnityEngine;

public class Token : MonoBehaviour
{
    //Creates a variable called tokenNumber for each token which I can change in the inspector
    [Header("Token Variables")]
    [SerializeField] public int tokenNumber;
}
